## 1. Contributors can detect frontend regressions offline

- [x] 1.1 Provide reproducible, non-watch frontend unit/component execution with isolated DOM state, existing JSX and path-alias support, explicit test discovery and failing outcomes for empty, focused or failing suites.
- [x] 1.2 Characterize existing pagination logic and accessible numbered-page interactions, including loading behavior, with meaningful regression-sensitive tests while preserving application behavior.
- [x] 1.3 Report frontend coverage including untested source, enforce the adopted new-AI line/branch floor when that scope exists, and retain absent AI scope as unmeasured without imposing a legacy coverage floor.

## 2. Contributors and CI include frontend tests in the shared result

- [x] 2.1 Include the full frontend coverage suite in the shared verification command with consistent focused execution and regression coverage of invocation and failure propagation.
- [x] 2.2 Document usable frontend commands, coverage interpretation and actual verification evidence across the engineering guidance and roadmap while identifying application smoke testing and backend AI coverage as pending.
