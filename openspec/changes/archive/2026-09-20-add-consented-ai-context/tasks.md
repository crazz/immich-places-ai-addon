## 1. Prepare only bounded consented context

- [x] 1.1 Bind each context class to current owner, installation, target, selection and provider consent while keeping Visual requests context-free.
- [x] 1.2 Preserve honest source-local capture-time status and enforce deterministic text, neighbor, time-window and total-context limits.
- [x] 1.3 Admit only currently eligible metadata sources with explicit lineage, selected-album scope and safe exclusion reasons.

## 2. Analyze one image against its exact evidence bundle

- [x] 2.1 Freeze independently owned context and reject source, consent or authority changes at dispatch/publication boundaries, including an explicitly empty bundle.
- [x] 2.2 Execute one bounded Context-assisted attempt with the existing provider format, reservation, cancellation and canonical validation guarantees.
- [x] 2.3 Reject invented sources and unsupported geometry/evidence claims without hidden repair or fallback calls.

## 3. Hand off private provenance without side effects

- [x] 3.1 Return immutable proposals with copied mode, context/consent versions, source records, digest and omission metadata for later durable use.
- [x] 3.2 Preserve safe diagnostics, zero research/persistence/Immich writes and unchanged Visual, capability, manual and GPX behavior under success, failure and concurrency.
