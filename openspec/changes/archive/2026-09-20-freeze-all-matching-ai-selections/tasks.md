## 1. Complete catalog selection under the declared scope

- [x] 1.1 Extend the protected preview with all-matching input and owner-qualified whole-catalog resolution after reconciling verified CH05 contracts; cover every scenario in **Resolve the complete authorized matching set**, including pages, combined filters, suppressed assets and ambiguous input, using policy, real SQLite and authenticated HTTP tests.

## 2. Honest counts and explicit batch limits

- [x] 2.1 Return exact shared-policy counts and bounded exclusion summaries for mixed, empty and duplicate-relation queries; cover every scenario in **Report exact query counts and bounded exclusions** with real SQLite fixtures and mode-specific response assertions.
- [x] 2.2 Enforce the eligible-asset cap and bounded complete enumeration without truncation or partial-success responses; cover every scenario in **Reject oversized matching batches without truncation**, including exact-cap/over-cap, many exclusions, deadline, cancellation and storage failure.

## 3. Durable frozen query membership

- [x] 3.1 Publish ordered membership and historical counts from one catalog transaction through CH05 storage; cover every scenario in **Freeze query membership from one consistent catalog state** with concurrent-sync barriers, rollback faults, reopen and current-member revalidation, proving later matches cannot expand a snapshot.

## 4. Shared lifecycle and integration evidence

- [x] 4.1 Preserve authority, mixed-mode quotas, expiry, installation invalidation, account deletion and zero external side effects; cover every scenario in **Preserve selection authority and side-effect boundaries** with authenticated fixtures and explicit/manual/GPX/gallery regressions.
- [x] 4.2 Record the synthetic large-catalog resolver timing, memory and deadline evidence; complete applicable shared checks, AI coverage, Go race tests, build, size/import, schema/reopen and GitNexus review from the design, and reconcile operator guidance and CH13 handoff without claiming unrun live or hardware acceptance.
