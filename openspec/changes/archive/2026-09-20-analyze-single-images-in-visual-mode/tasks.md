## 1. Complete one bounded Visual attempt through the canonical contract

- [x] 1.1 Produce a minimized strict/explicit-JSON request from one bound prepared image and normalized requested languages, preserving the single canonical schema and existing capability codecs.
- [x] 1.2 Accept only complete ordinary provider output through canonical Visual validation, preserving located/ambiguous/unknown outcomes, language statuses and bounded optional usage in an immutable handoff.
- [x] 1.3 Require current caller authority and one explicit dispatch reservation, enforcing deadlines, cancellation, safe failures and no hidden downgrade, repair or retry.

## 2. Enforce current image and private provider authority end to end

- [x] 2.1 Bind the internal attempt to current owner/installation/asset/source access and exact enabled provider revision with applicable read-only capability observations, including historical revisions without silent redirection.
- [x] 2.2 Recheck authority at transmission and publication boundaries, preserve zero persistence/Immich mutations and document the internal caller, evaluation and live-verification limits while keeping existing workflows usable.
