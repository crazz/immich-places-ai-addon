## 1. Complete bounded provider output becomes a typed proposal

- [ ] 1.1 Deliver **Enforce the canonical bounded result contract** with the owner-approved validator, one embedded canonical schema and strict bounded parsing; automate every canonical-fixture, malformed/ambiguous, numeric/resource-limit and transport-completion scenario, including constructor failure and denied external schema loading. Preserve the canonical bytes/version and reconcile its relocated references.

## 2. Outcomes and references preserve their actual meaning

- [ ] 2.1 Deliver **Keep outcomes consistent with candidate selection**, covering every located, ambiguous, unknown and contradictory-selection scenario without automatically choosing or repairing a result.
- [ ] 2.2 Deliver **Preserve separate camera and subject coordinates**, covering every subject-only, zero and exact/out-of-range boundary scenario through typed conversion without coordinate substitution or precision-induced acceptance.
- [ ] 2.3 Deliver **Resolve identifiers only within authorized evidence**, covering every local-reference, duplicate/dangling ID, foreign/invented source, Visual provenance and Context-assisted source-connection scenario with per-request synthetic authorized bundles.

## 3. Uncertainty and direction retain evidence constraints

- [ ] 3.1 Deliver **Require consistent and conservative radius claims**, covering every supported/null radius, contradictory precision/granularity and evidence-basis scenario without claiming calibrated accuracy or changing an invalid value into an accepted one.
- [ ] 3.2 Deliver **Require a reviewable viewpoint and evidence for direction**, covering every nullable, supported and invalid heading scenario, including method-specific evidence, exact angular bounds and subject-bearing-only rejection.

## 4. Every requested language has a consistent status

- [ ] 4.1 Deliver **Cover the requested language set exactly**, covering every non-English/partial-unavailable, normalization/duplicate, incomplete/contradictory entry and invalid server-context scenario without fallback languages or synthesized text/status.

## 5. Later consumers receive safe results and verified integration boundaries

- [ ] 5.1 Deliver **Return safe immutable validation outcomes without authority**, covering every envelope-injection, bounded/redacted deterministic failure and independent side-effect-free result scenario; verify context/result copying, zero-value rejection, concurrent validation and protected package dependencies with synthetic consumer fixtures.
- [ ] 5.2 Establish implementation readiness evidence through all applicable shared checks, AI coverage, race/fuzz regression, representative and maximum-bound validation measurements, schema/document links, pinned-dependency vulnerability verification and standalone package-test packaging in the backend container context described in the design; reconcile semantic guidance and CH09/CH11 handoff, reporting actual outcomes without claiming live provider compatibility or location accuracy.
