## 1. Launch and review a useful approximate location

- [x] 1.1 Support version 2.0 Research estimates alongside unchanged v1 results, including 500-meter, multi-kilometer and city/region radii, unknown error and ambiguous alternatives without a precision or confidence cutoff; retain structural and coordinate validation (ai-location-proposals; WR01–04).
- [x] 1.2 Connect Research launch to the existing configured provider, durable admission and atomic history so one Start produces an immutable approximate result without a search-metadata test, new service or proxy extension (PC01–05; JB01–03, JB07–08).
- [x] 1.3 Present proposed camera coordinates and estimated error prominently with concise explanation and usable alternatives, including keyboard/narrow-screen and degraded-map use, while keeping review free of writes (RV01–04; LP08–09).

## 2. Use hints and retain useful answer references

- [x] 2.1 Carry the exact displayed hint and optional album/date inputs through admission, idempotency, context freezing and the Research prompt; challenge misleading hints and preserve Visual/Context-assisted behavior without adding hidden neighbors (WR03, WR08–09; LP07; JB01–03).
- [x] 2.2 Accept and persist bounded answer-provided source URLs and relevance explanations without prior backend source approval or search telemetry; keep valid estimates when sources are absent or individual links are unusable (WR05–07; LP02–03; JB07).
- [x] 2.3 Make safe references accessible from result detail with inert text, deliberate external navigation and no automatic source fetching; preserve old evidence presentation and prevent account-crossing disclosure (RV05–09; ai-results-and-review legacy scenarios).

## 3. Keep longer investigations bounded and history durable

- [x] 3.1 Support a server-selected finite Research deadline consistently across worker, analysis and transport while preserving Visual/capability deadlines, lease renewal, ordinary single-dispatch defaults and explicit operator restrictions (JB04–06; PC03–05; WR10).
- [x] 3.2 Preserve exact authority, atomic completion, owner isolation, cancellation fencing and no automatic retransmission under ordinary defaults across restart, source loss and history/account cleanup (JB05–09; WR08–10).
- [x] 3.3 Preserve v1 and v2 results through real SQLite upgrade/reopen, execution disablement and compatible rollback, without a search-source table or destructive result rewrite (LP01; JB07–09; RV07–08).

## 4. Establish compatibility and compare useful estimates

- [x] 4.1 Provide a reproducible comparison report for matched approved photos/hints, retaining coarse, unknown and failed outcomes and separating model-estimated error from independently measured camera error without an acceptance cutoff (WR11–12).
- [x] 4.2 Establish the actual configured proxy's request/answer and duration behavior using authorized synthetic/public inputs; record supported behavior or concrete limitations without claiming telemetry is required or silently adding infrastructure (PC01–03; design Decisions 1 and 5).
- [x] 4.3 Complete scenario-to-test evidence in verification-plan.md and the implementation verification record, including applicable size/import, lint/type, race/SQLite, coverage, build, container and browser gates; report unavailable live checks honestly and retain the accepted planning-baseline reconciliation (design Decisions 7–8; WR11–12).
