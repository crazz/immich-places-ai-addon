## 1. Inspect a fresh single-photo GPS comparison

- [ ] 1.1 Produce a read-only preview from an owned staged draft using fresh authorized source/GPS values, exact analyzed-asset scope and no quality threshold (P01–P03, P06).
- [ ] 1.2 Present complete before/after and changed/unchanged values in the AI UI with numeric keyboard access, clear photo identity and no manual-save side effects (P10, P14–P16).

## 2. Resolve stale source and GPS decisions before proceeding

- [ ] 2.1 Detect changed GPS and material source changes, preserve the saved draft and provide explicit baseline/current-image review recovery without silently accepting new before-values (P04–P06).
- [ ] 2.2 Reject obsolete draft/authority publication and plan substitution, including concurrent edits, lost access, changed credentials and foreign identifiers (P08–P09, P18).

## 3. Retain and expire the exact private preview

- [ ] 3.1 Persist immutable revision-bound plans and digests with stable reload/restart behavior, scope isolation and bounded payloads (P07–P09, P17–P18).
- [ ] 3.2 Expose expiry/invalidation and bounded preview capacity/cleanup without extending old validity or deleting drafts and protected history (P11–P13).
- [ ] 3.3 Keep read-only preview use available with execution disabled, enforce account/installation lifecycle and prevent late private replies from becoming usable in another view (P16–P18).
