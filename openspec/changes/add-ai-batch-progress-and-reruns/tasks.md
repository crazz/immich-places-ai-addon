## 1. Launch an explicitly authorized frozen batch

- [ ] 1.1 Preview selected, current-page and all-matching intentions with authoritative counts, exclusions, expiry and invalidation of changed or unsupported scope.
- [ ] 1.2 Confirm exact provider readiness, mode, format, languages, finite limits and separate image/context disclosure, resetting confirmation when inputs change.
- [ ] 1.3 Admit either supported mode atomically with immutable selection/consent provenance and idempotent acknowledgement recovery.

## 2. Complete durable Context-assisted analysis

- [ ] 2.1 Freeze and retain only the authorized bounded context bundle, preserving source lineage, omissions and privacy through restart and retries.
- [ ] 2.2 Publish Context-assisted results only after current authority, budget and exact evidence validation while preserving existing Visual behavior and history.

## 3. Observe and control durable work

- [ ] 3.1 Restore job progress across navigation with bounded polling, safe stale/error states, distinct execution/outcome labels and owner-isolated response handling.
- [ ] 3.2 Cancel mixed jobs explicitly without losing completed results, recalling already transmitted data or triggering extra provider calls.
- [ ] 3.3 Keep launch/progress keyboard-operable and preserve read/cancel, manual and GPX behavior when execution is disabled, with no draft or Immich-write authority.

## 4. Start explicit reruns without replacing results

- [ ] 4.1 Create linked retry-failed runs from only eligible failed parent members after fresh preview and consent.
- [ ] 4.2 Create reanalysis runs with new choices and exact validated parent membership while retaining every earlier result and rejecting foreign or stale lineage.
