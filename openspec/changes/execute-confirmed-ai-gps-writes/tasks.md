## 1. Confirm one exact GPS plan and recover its identity

- [ ] 1.1 Deliver protected durable confirmation of an owned current preview, with exact plan binding, one-time consumption and stable idempotency across duplicates, storage failure and restart (W01–W04).
- [ ] 1.2 Provide accessible exact-plan confirmation and lost-acknowledgement reconciliation on HTTP origins, preserving private view boundaries and manual pending choices (W02–W03, R04–R05).
- [ ] 1.3 Keep real dispatch disabled by default and reject unsupported adapter/version configurations while preserving local review/status access (W08, W26).

## 2. Execute one approved GPS step under fresh authority

- [ ] 2.1 Reserve and send only the exact single-asset GPS pair after fresh source/baseline/authority checks, preserving zero values and all unrelated fields, with a verified no-op for unchanged plans (W05–W06, W09–W11).
- [ ] 2.2 Prevent hidden transport/browser retries, redirects and alternate-route fallback, with every potentially sent request covered by a durable attempt reservation (W07).
- [ ] 2.3 Serialize same-target AI writes across accounts and worker generations, preserving independent-target progress and private contention responses (W12–W13).
- [ ] 2.4 Invalidate queued or safely retryable approval on draft edits and protect a revision while its reserved write can still act, preserving unsaved user changes (R01–R02).

## 3. Reconcile uncertain outcomes before another attempt

- [ ] 3.1 Recover potentially sent operations through bounded authorized readback after lost responses, restart or expired leases, retaining unresolved/conflict states without blind resend (W13–W15, W18).
- [ ] 3.2 Permit only a bounded explicit retry after known completion, unchanged fresh baseline and current approval/authority; reject exhausted or competing retries without resetting attempt counts (W16–W17).
- [ ] 3.3 Offer read-only status reconciliation and distinct recovery feedback without automatic provider calls or false saved labels (W15, W17–W18, R06).

## 4. Publish verified GPS and retain its independent history

- [ ] 4.1 Verify source-consistent GPS using the documented numerical tolerance and recover local storage/refresh failure without repeating a remote mutation (W19–W20, W22).
- [ ] 4.2 Publish verified local GPS in coordination with catalog synchronization, refresh counts/markers only after durable verification and preserve AI result/draft/audit history after Missing GPS membership changes (W21–W22, R03).
- [ ] 4.3 Display the exact approved revision, before/intended/observed values and distinct no-op, write, conflict, failed and unresolved outcomes with accessible private UI state (W24, R03–R06).

## 5. Preserve write authority and private audit through lifecycle changes

- [ ] 5.1 Stop new sends on disablement, shutdown, account deletion and installation changes while retaining recoverable uncertain reservations and preventing late private-data recreation (W10, W13, W23, W25).
- [ ] 5.2 Preserve owner-isolated approval/attempt/outcome history and referenced plans through migration, reopen and ordinary cleanup without private payloads in logs (W01, W24–W25).
- [ ] 5.3 Keep live write rollout unavailable until authorized disposable-fixture compatibility is recorded, with explicit disabled-state behavior and rollback that retains audit without claiming to undo upstream GPS (W08, W26).
