# CH19 implementation verification

Status: complete — implemented, verified and synchronized. Base `c409835a24903280ecef562ec885ecc90c3bdc3c`; current local branch `codex/plan-ai-selection-and-validation`. Inline implementation and self-reviews; no subagents. Live GATE-02 is unverified; ordinary dispatch remains disabled. No archive, push, deployment or live calls authorized.

## Workflow

- [x] Required standards, prerequisite contracts, artifacts and verification plan read; OpenSpec ready, 0/16 tasks.
- [x] Dependencies reconciled: sequential slices 1 → 2 → 3 → 4 → 5, with durable storage introduced before any dispatch. Migration tip 027. Existing integration files confirmed in the current checkout; writer package/transport/runtime are new.
- [x] Slice 1: durable confirmation and accessible identity recovery.
- [x] Slice 2: exact guarded dispatch and revision protection.
- [x] Slice 3: bounded read reconciliation and explicit retry.
- [x] Slice 4: verified catalog publication and independent audit UI.
- [x] Slice 5: lifecycle, migration and rollout controls.
- [x] Whole-change reviews, thirteen gates and both capability syncs; this record accompanies the authorized local commit.

Conventions for every slice: English; 500 physical lines; pure focused AI core, root persistence/composition adapters and AI feature UI; short owner/installation-qualified SQLite transactions without network; immutable canonical approval; exact nullable GPS and valid zero; no stack expansion/legacy retries; current authority and generation fences; safe errors without payload logs; bounded I/O/concurrency/storage; one-test RED/GREEN/refactor cycles (existing behavior may be characterized); actual file-backed SQLite and recording synthetic HTTP evidence; private accessible UI preserving manual work; source dependency and measured coverage gates. No extra permission needed for the authorized inline implementation, synchronization or commit.

## GitNexus

Bound to this exact checkout and base commit, current index with no incomplete reasons. Graph limitations include bounded process enumeration, Go interface dispatch and unsupported Go route extraction; source and protected handler/browser tests remain mandatory. Initial newAIResultHandler/aiResultStore impact LOW, composition-root caller main. No writer capability is passed to analysis, drafts or previews.

## Development ledger

Logs: ignored `out/checks/ch19/`, pinned environment copied from CH18. Test names/count changes, actual RED/GREEN results and refactor assessments are recorded below as executed.

W08 (approval test file 0 → 1): missing route 404 RED, default disabled route 503 GREEN with preview regressions. Refactor: dedicated registration keeps the new boundary separate. W01/W02 (1 → 2): explicit enabled fixture still returned 503 RED, atomic approval/protected plan/guard/event and restart/by-key/same-key retrieval GREEN. Test setup initially omitted origin and retained a closed handler database after reopen; corrected before claiming behavior evidence. Refactor: split HTTP, SQL confirmation, private read and core DTO responsibilities. New writer symbols are UNKNOWN until indexed; source confirms only dedicated result-handler registration reaches them.

W03 (2 → 3 approval tests): wrong digest/expired/edited/rejected/substituted plans accepted RED, strict canonical digest/scope/current revision/consumption checks GREEN. Unknown/oversized bodies and foreign keys also checked. Refactor: pure DecodePlan owns canonical validation; SQL owns authorization/consumption. W04 (3 → 4): transaction-failure/reopen characterization PASS; failing audit insert rolls back operation, target guard and protection together. No refactor needed: the existing short transaction is the failure boundary.

W05/W06 transport (0 → 1): zero recorded requests RED, one exact PATCH with only latitude/longitude and valid zero GREEN. W07 (1 → 2): recording real-socket redirect/drop/429/5xx characterization PASS with one send per attempt. Refactor: dedicated HTTP client has no reusable connection, replay body or idempotency header; completion uncertainty remains explicit. No legacy transport dependency.

Exact execution W05 (execution tests 0 → 1): queued state/no send RED; real source read → durable reservation → exact transport → source-consistent readback → local publication GREEN. Changed stack primary and six-member stack preserve the sole approved asset. Core executor receives consumer-owned ports; SQL and sync stay in root adapters. Refactor: source decoding, reservation/sending and publication are separate. Shared aiImageSourceDigest impact CRITICAL (19 symbols/10 process groups); its original primary-only contract remains intact, while writeback explicitly selects exact-asset validation. Graph refresh initially failed on the sandboxed registry lock; rerun with the authorized filesystem access.

W09/W10 (freshness 0 → 1): current GPS/image/hidden/trashed/key/expiry/disable/installation characterization PASS, zero sends. R01/R02 (revision 0 → 1): queued approval survived edits and reserved revisions remained editable RED; transaction-local revision guard GREEN with all draft regressions. Refactor: root guard owns cross-resource SQL, draft core exposes only its error; no mutation dependency is added to drafts. Snapshot impact CRITICAL and error mapping HIGH reported before edits. Draft edits also invalidate retained previews without changing their canonical payload.

W14 (recovery 0 → 1): reopen left applied lost-response operation unprocessed RED, existing operation readback/local publication GREEN with one lifetime send. W15/W18/W19 (1 → 2): mismatched readback errors and exact-only equality RED, pure readback decision with 1e-7 degree tolerance GREEN; unknown baseline remains verifying, completed rejection becomes retryable, third values/source replacement conflict. Uncertain completion retains target exclusion even after observing intended GPS. Refactor: readback policy is independent of SQL/HTTP. Bounded read recovery (2 → 3): five automatic reads RED, three persisted recovery attempts GREEN; budget does not reset after expiry and mutation count remains one. New graph symbols still returned UNKNOWN; actual writer/core callers were verified in source and another refresh initiated.

W16/W17 (retry 0 → 1): protected retry route missing RED; eight simultaneous explicit requests produce one next generation, two lifetime sends maximum, and unknown completion rejects retry GREEN under race instrumentation. Refactor: persisted retry generation is independent of the attempt count. W20/W22 (publication 0 → 1): missing catalog row prevented independent upstream verification RED; owned current credentials plus exact upstream identity permit read-only verification and pending local refresh GREEN, without fabricating a row or resending after storage failure. Authority checks still reject existing hidden rows and credential/installation changes.

W13 (concurrency 0 → 1): expired lease stole an active sender RED; in-process target/owner guard GREEN under race detector, with two global slots. Runtime lock (0 → 1): a second handle acquired lock RED; OS-exclusive lock GREEN, reacquisition after close verified. Unsupported OS builds fail closed. The incremental graph again returned malformed symbol associations; force rebuild completed rather than relying on them.

Private operation history (0 → 1): missing GET collection returned 405 RED; owner-qualified bounded summary history GREEN, with foreign draft rejection and no-store. UI confirmation (0 → 1): missing Confirm control RED; exact-plan keyboard confirmation and same-key GET reconciliation after lost acknowledgement GREEN with crypto.getRandomValues on HTTP origins and redirect:error. Status/retry/poll/refresh/repeated-confirmation/settlement cycles (1 → 6): missing controls or outcome distinctions RED, individual focused GREEN runs. The history belongs to owner/draft, independently of displayed revision or catalog membership; only identifiers and digest are retained for an unresolved confirmation. Provider/manual save helpers are never used.

Canonical operation reload (integrity 0 → 1): corrupt operation digest was returned RED; DecodePlan plus owner/installation/preview/draft/revision/target checks GREEN before reads or dispatch. Initial NUL-byte corruption setup hit SQLite's length constraint; replaced with a valid-length wrong digest before recording the behavioral RED. Graph risk CRITICAL for shared operation reading and HIGH for the operation DTO was reported and callers verified in source.

W23 preflight disablement (0 → 1): queued approval survived disable RED; generation-conditional cancellation GREEN without releasing an already-reserved guard. W11 no-op characterization PASS: fresh readback, durable verified local publication and zero mutation attempts. Sync drain failure (0 → 1): missing pending-state reason RED; fenced bounded cleanup records LOCAL_REFRESH_PENDING and later read-only repair GREEN. W21 stale sync characterization PASS under race instrumentation: the real pause/drain lock orders an older local sync publication before final upstream readback/verified publication, then releases; existing pause/settings regressions also passed.

W25 deleted sender (0 → 1, completed/unknown cases): definite completed sender retained its opaque guard RED; conditional orphan-guard retirement GREEN. Private operation/target/event/preview/draft rows stay deleted and the other account survives; unknown completion retains only opaque exclusion. Shutdown characterization PASS: cancellation interrupts an active HTTP sender and preserves its possibly-sent reservation. Composition now waits for writer shutdown within seven seconds before database teardown.

W12 concurrent owners characterization PASS with two independently accepted/staged/previewed plans in one real SQLite database: one confirmation, generic TARGET_BUSY for the other owner, foreign retrieval denied, independent targets admitted within two global slots. Corrected the synthetic analysis fixture's one-item call budget before recording the pass. W13 reserved/sent/verifying reopen table PASS under race instrumentation: generation advances, recovery only reads, late sender/completion cannot change newer state or resend. Recovery scheduling (0 → 1): immediate recovery before backoff RED; persisted one-second initial due time GREEN followed by existing 5/15-second bounded recovery tests.

Migration characterization PASS for 025/026/027 upgrades to 028, repeated migration, atomic failing 027→028 upgrade, new post-upgrade confirmation/reopen and four simultaneous connections enforcing foreign keys. W24 retention characterization PASS: completed immutable audit, preview, draft and analysis survive cleanup, catalog reset and disabled reopen with one lifetime send. W10 last-check table PASS after reservation for disable, expiry, visibility, key, installation and deletion. W19 settlement contract (0 → 1): absent settlement signal RED; verified GPS and sender settlement returned independently GREEN. The UI explains continued exclusion after observed GPS with unknown sender completion.

First full frontend regression run: 129/130 tests passed; the old result-header assertion still expected unconditional “Write: not requested”. The header now reports review only and the independent operation component supplies write status. Corrected that obsolete assertion and strengthened the preview test to await the new confirmation control while asserting no POST before explicit confirmation; focused regressions passed. Type checking identified two incomplete test mock signatures, corrected with complete typed fixtures. These early runs are not the final gates.

Final boundary additions: R02 unsaved editor/WRITE_IN_PROGRESS characterization passed (6 ResultDraft tests); safely retryable approval cancellation and installation rotation during a barrier-controlled active sender passed with race instrumentation. Browser verification initially found the manual pending label hidden in the mobile-only gallery layout; restoring desktop width after the narrow dialog inspection confirmed the same pending edit remains visible. The complete focused journey passes with one exact mutation and no additional provider call. Narrow screenshot inspected: readable wrapped numeric/text controls, no horizontal overflow.

History accessibility review found the API's continuation cursor lacked a UI control. One pagination scenario failed for the missing control, then passed with bounded page replacement, return-to-first control and retained inspected operation. Refactor assessment: a small private read action in the existing operation session preserves the owner/result abort boundary without another shared state layer. Lint found unused typed mock parameters and a missing local test-helper return type; corrected without changing assertions. Final lint passes with only the three inherited warnings.

Transport bounds characterize cancellation, excessive response headers and bounded response-body draining with one lifetime request. An initial local test server waited indefinitely for request cancellation without consuming the request; an explicit test-only release channel now makes teardown deterministic. The full early backend run found the inherited migration-bootstrap assertion still expected 027; it now expects 028 and its focused regression passed. Neither failed early run is reported as a passing gate.

## Scenario evidence

All backend names below are executable Go tests in `backend/aiWrite*test.go` unless a package is specified. They use temporary real SQLite, synthetic HTTP and explicit clocks/barriers. UI references are co-located Vitest tests; the browser journey uses the built application and loopback services.

| Scenario | Automated evidence |
|---|---|
| W01 | `TestAIWriteConfirmationSurvivesReopenAndConsumesExactPlan`; `TestAIWriteUpgradeFromPrerequisitesAndPooledOwnerConstraints` |
| W02 | `TestAIWriteConfirmationSurvivesReopenAndConsumesExactPlan`; `WriteOperation.test.tsx` keyboard lost-acknowledgement and repeated-identity tests; `tests/e2e/writeback.spec.ts` |
| W03 | `TestAIWriteRejectsSubstitutedStaleOrConsumedApproval`; `TestAIWriteRejectsCorruptStoredOperationBeforeReadOrDispatch` |
| W04 | `TestAIWriteApprovalFailureRollsBackAllAuthority` |
| W05 | `TestAIWriteExecutesReservedExactGPSDespiteChangedStack`; adapter `TestExactSingleAssetGPSOnly` |
| W06 | The same execution and exact-payload tests preserve latitude zero and omit every non-GPS field |
| W07 | Adapter `TestMutationNeverRetriesRedirectsDropsOrFailures`, `TestMutationTimeoutAndResponseBoundsNeverResend`; browser one-POST lost-acknowledgement assertion |
| W08 | `TestAIWriteDisabledKeepsLocalPreviewReadable`; `TestAIWriteDefaultAndRuntimeGatesFailClosed`; unsupported-profile approval case |
| W09 | `TestAIWriteFreshChangesCannotSend` GPS/image cases |
| W10 | `TestAIWriteFreshChangesCannotSend`; `TestAIWriteRechecksAuthorityAfterReservationBeforeTransport`; `TestAIWriteExpiredReservationCannotStartTransport` |
| W11 | `TestAIWriteUnchangedPlanVerifiesWithoutMutation` |
| W12 | `TestAIWriteConcurrentOwnersShareTargetExclusionWithoutPrivateDisclosure` |
| W13 | `TestAIWriteActiveSenderCannotBeStolenAfterLeaseExpiry`; `TestAIWriteReopenEveryReservedPhaseFencesLateWorkers`; `TestAIWriteRuntimeLockExcludesOtherProcessHandles`; `TestAIWriteRuntimeSweepsDurableQueueAndStops` |
| W14 | `TestAIWriteRecoversAppliedLostResponseAfterReopenWithoutResend` |
| W15 | `TestAIWriteReadbackDistinguishesUncertaintyConflictAndTolerance`; `TestAIWriteReadOnlyRecoveryIsBoundedAndNeverResendsUnknownBaseline`; `TestAIWriteCheckStatusStartsOnlyBoundedReadsWhileDisabled`; `TestAIWriteFirstRecoveryWaitsForPersistedDeadline` |
| W16 | `TestAIWriteExplicitRetryIsIdempotentBoundedAndRequiresKnownCompletion` completed-response case |
| W17 | The same eight-request retry race, exhausted second attempt and unknown-completion cases; post-reservation authority/lease tests |
| W18 | `TestAIWriteReadbackDistinguishesUncertaintyConflictAndTolerance` third-value and changed-source cases |
| W19 | Rounded/outside-tolerance readback cases; `TestAIWriteReportsObservedGPSSeparatelyFromSenderSettlement`; UI unknown-settlement warning |
| W20 | `TestAIWriteLocalFailureRecoversReadOnlyAndMissingRowIsNotFabricated` storage failure case |
| W21 | `TestAIWriteDrainsStaleSyncBeforePublishingVerifiedGPS`; `TestAIWriteSyncDrainFailureRemainsPendingAndRecoversWithoutResend`; existing sync/settings regressions |
| W22 | Missing-row publication case; `VerifiedGPSRefresh.test.tsx`; built-browser Missing GPS removal with retained AI history |
| W23 | `TestAIWriteDisableCancelsQueuedAuthorityWithoutReenableSend`; `TestAIWriteDisableDuringPreflightRetiresQueuedApproval`; `TestAIWriteShutdownRetainsPotentiallySentReservation` |
| W24 | `TestAIWriteAuditSurvivesCleanupCatalogResetAndDisabledReopen`; `TestAIWritePrivateHistorySurvivesReloadAndRejectsForeignDraft`; migration/reopen; UI paged audit and disabled browser reload |
| W25 | `TestAIWriteDeletedOwnerNeverReappearsAfterSenderCompletion`; `TestAIWriteAccountDeletionKeepsOnlyUncertainOpaqueGuard`; `TestAIWriteInstallationRotationFencesActiveSenderPublication` |
| W26 | `TestAIWriteDefaultAndRuntimeGatesFailClosed`; `scripts/checks/ai-write-fixture.test.mjs`; network-disabled container startup. Live GATE-02 NOT RUN |
| R01 | `TestAIWriteDraftEditCancelsQueuedButProtectsReservedRevision`; `TestAIWriteRetryableApprovalIsCanceledByDraftRevision`; rejected-preview approval case |
| R02 | Reserved-revision case; `ResultDraft.test.tsx` preserves unsaved latitude/longitude after `WRITE_IN_PROGRESS` |
| R03 | `tests/e2e/writeback.spec.ts`; `VerifiedGPSRefresh.test.tsx`; audit retention/reopen tests |
| R04 | Built HTTP-origin keyboard/narrow/tile-failure journey; component same-key GET and explicit repeat tests |
| R05 | Built journey's independent manual edit; component disabled overlapping confirmation and late owner/result reply fencing; existing draft/preview manual-conflict tests |
| R06 | Component unresolved/local-refresh/known-failure/retry/settlement tests; backend readback matrix and read-only repair |

## Inline reviews

Each slice was reviewed in order against its requirements, then TDD evidence, then construction/ownership/error boundaries. These are inline self-reviews under the user's explicit no-subagent instruction, not independent reviews.

1. Durable confirmation: exact owned immutable plan, strict input, atomic protection/guard/audit and same-key recovery comply with W01–W04; storage rollback and reopen tested. No mutation capability enters acceptance/preview. Focused approval/UI/config gates passed. No outstanding finding.
2. Exact execution: source/baseline checks, reservation, last authority/lease fence and single GPS transport comply with W05–W13/R01–R02. Shared image-digest wrapper preserves primary-only analysis behavior; only the writer accepts the exact non-primary target. Revision guard is in the snapshot transaction. Focused execution/transport/authority/concurrency/revision gates passed. No outstanding finding.
3. Recovery: generation claims never resend; uncertainty retains target exclusion; only explicit known-completion retry can consume the second attempt. Persisted backoff/read budget and status-only recovery are distinct from mutation budget. Focused recovery/retry/schedule/generation gates passed. No outstanding finding.
4. Publication/UI: real sync drain precedes final read and short atomic local publication; missing rows remain missing; verified upstream state, local refresh and sender settlement are separate. The callback refreshes catalog/health without touching pending locations. Audit pagination finding fixed with RED/GREEN evidence. Focused publication/sync/components/browser gates passed. No outstanding finding.
5. Lifecycle/rollout: migration 028, account/installation fences, bounded shutdown and exclusive runtime lock preserve unresolved work without recreating private rows. Startup switches default off/unsupported empty profile. Synthetic fixture opt-in is tested; no live runner was added or invoked. The planned “live-runner guard” is realized by the installed synthetic fixture control and the explicit operator live-check procedure, with actual live evidence still unavailable. Focused migration/lifecycle/runtime/config gates and backend container build/startup passed. No outstanding finding.

Whole-change source review confirms focused pure core, dedicated transport, root SQL/sync composition and feature-owned UI; no new dependency, service, manual/GPX mutation path or architectural departure. All network reads occur outside SQLite write transactions. New handlers preserve the existing authenticated/no-store wrapper and origin guard. Logs/errors introduced by the writer contain safe state codes, not payloads. Strict client validators check stored scope and finite values; no provider or legacy retry helper is invoked by confirmation. Long operation-session rendering was reviewed for private abort fencing, bounded polling and explicit-only mutation actions; splitting it solely for size would add indirection without a new responsibility.

## Final verification and analysis

Base remained `c409835a24903280ecef562ec885ecc90c3bdc3c` on the original local branch. No worktree or branch switch occurred. The pre-existing untracked `.brooks-lint-history.json` is excluded from staging.

Forced GitNexus refresh indexed this checkout at the base revision with current uncommitted source: 12320 nodes, 36302 edges and 626 reported flows. Its flow generator remains bounded (dropped entry points/branches and cross-language field gaps); no absent flow is treated as proof of safety. Staging exposed new files to `detect_changes(scope=all)`: 90 files, 421 changed symbols and 73 affected processes, all 421 symbols returned, no partial/truncated result. Overall risk is CRITICAL due to shared image validation, draft snapshots and composition; these callers/processes were inspected and covered by image/context/draft/full application regressions. The import-cycle check enumerated completely with zero cycles. Go route/shape extraction returned no supported route nodes; source-qualified handlers, strict DTO validators, authenticated handler tests and the built proxy journey supply that evidence. No route-tool pass is claimed.

Initial full cumulative invocation passed the backend race suite (root 488.607 seconds), all compiled internal packages and AI statement coverage **87.75% (5287/6025)**. The new pure writeback package is covered through root integration in the combined `-coverpkg=./...` profile; its standalone “no test files / 0%” line is not the combined measurement. Frontend coverage passed **135 tests across 55 files**. Aggregate AI subtree coverage from `coverage-summary.json` is **93.65% lines (1180/1260)** and **83.55% branches (1823/2182)**, both above 80%. This initial invocation had an earlier lint failure; corrected lint, size, typegen and types were rerun successfully. Final cumulative closure is recorded below when complete.

Backend image build `docker build -f backend/Dockerfile -t immich-places-ch19-check backend` passed. A disposable `--network none` container started with synthetic settings and no write-enable/profile override, initialized SQLite, listened and shut down cleanly on TERM (exit 0). The first wrapper expected GNU timeout's 124 although BusyBox returned the backend's graceful 0; corrected the wrapper to record that actual exit. No deployment, push, live photo or live service was used.

OpenSpec Plus specification review reused the approved proposal/design, all nine requirements and 32 GIVEN/WHEN/THEN scenarios. No new scope, decision, ambiguity or missing success/authorization/failure behavior was found. Existing user authorization covers both main-spec merges; the user's inline/no-subagent direction governs review. Maintained-spec format and existing purposes/requirements remain authoritative; delta operation headers are not copied. Both selected paths come from the status JSON and one current specs-rule snapshot was read before writing. Pre-sync strict validation passed all 12 current items.

Both capability deltas were synchronized after passing deterministic verification: seven added requirements / 26 scenarios into `ai-immich-writeback`, and two added requirements / six scenarios into `ai-results-and-review`. All pre-existing main-spec bytes through their final requirements were preserved; existing purposes were unchanged. Strict maintained-spec validation passed all eight capabilities. No archive was performed.

The cumulative rerun passed all thirteen gates, including 3 legacy and 16 AI browser journeys. A final cross-view audit then found the inherited result-list row still hard-coded “Review: unreviewed · Write: not requested” for every result. The detailed operation view was correct, but returning to the list could contradict it. A new R03/R06 test observed that RED; the row now displays its saved review state and directs a draft owner to GPS status in the result, keeping the dedicated operation resource authoritative. Entries without any draft still show not requested. All three list tests passed. GitNexus impact was LOW through ResultsSession/AIResultsWorkspace/ResultNavigation. Refactor review: direct use of the already validated draft/review projection avoids an unnecessary second operation-history query per list row. Cumulative verification is repeated for this final presentation change.

Final closure on 25 September 2026: `bun run check --base c409835a24903280ecef562ec885ecc90c3bdc3c` exited **0**, with **all thirteen gates passing** on the final source (`out/checks/ch19/full-gates-closure.log`). Checker regressions: 73 passed. Frontend: **136 tests / 55 files passed**; aggregate AI line coverage **93.65% (1180/1260)** and branch coverage **83.61% (1826/2184)**. AI Go statement coverage remains **87.75% (5287/6025)**; the final rerun reused valid Go test cache from the preceding instrumented pass because backend source/tests were unchanged. Both application builds pass. Browser smoke: **3 legacy + 16 AI journeys passed**, zero retries. Lint has only the three documented inherited warnings; the inherited Next.js middleware deprecation notice is unchanged. Size, dependencies, gofmt, typegen, types, Go vet and builds all pass.

Strict OpenSpec validation passes all **12 specs/changes**, including **8 maintained capabilities**. Exact merge verification confirms all previous CH16/CH18 main-spec content is preserved and all nine added requirements / 32 scenarios match their approved deltas. Local-link and whitespace checks pass. Final source index contains 12364 nodes / 36352 edges; complete staged change analysis covers **97 files / 482 symbols / 73 processes**, with no partial/truncated response and the same reviewed CRITICAL shared-boundary risk. Source-based dependency gates supplement the documented graph route/flow limitations. All 16 implementation tasks are checked and inline review findings are resolved.

**Remaining release limitation:** live GATE-02 was **not run**. No actual Immich version/rights/field-preservation/sidecar compatibility is claimed. Ordinary `AI_WRITE_ENABLED=false` and empty unsupported default profile remain unchanged; only explicit disposable local fixtures enabled sends. No live provider/Immich call, private-photo write test, deployment, archive, push, CH20 work or codex-proxy change occurred. The existing untracked `.brooks-lint-history.json` remains untouched and excluded.
