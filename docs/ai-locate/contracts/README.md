# Analysis result contract

The single [canonical v1 schema](../../../backend/internal/ai/results/ai-analysis-result.v1.schema.json) lives in the backend results package so it can be embedded without runtime file or network access. The relocation preserves its version and bytes. [Examples and semantic guidance](../examples/SEMANTIC_VALIDATION.md) remain alongside the planning documents.
